# DNS & GitHub Pages verification report

## DNS records

| Record | Status | Value |
|--------|--------|-------|
| `lewiseydman.dev` A | OK | 185.199.108–111.153 (all 4 GitHub Pages IPs) |
| `www.lewiseydman.dev` CNAME | OK | `lewiseydman.github.io` |
| `lewiseydman.dev` AAAA | Not set | (optional; GitHub AAAA is optional) |

## Live site check

- HTTP apex (`http://lewiseydman.dev/`) responds **200 OK** from GitHub Pages.
- HTTPS (`https://lewiseydman.dev/`) still fails with a certificate error: GitHub has not yet finished provisioning the SSL certificate for the custom domain. This is expected immediately after DNS verification and usually resolves within a few minutes to an hour.

## Repo-side configuration

- `vite.config.ts` correctly switches to static prerendering when `GITHUB_PAGES=true` is set.
- `package.json` has the matching `build:pages` script (`GITHUB_PAGES=true vite build`).
- `.github/workflows/deploy.yml` handles the static build, `404.html` fallback, `.nojekyll`, and writes a `CNAME` file from the `CUSTOM_DOMAIN` repository variable.
- `public/CNAME` is intentionally not present in the repo; it is generated by the workflow, so the only remaining repo-side check is whether the GitHub Actions variable `CUSTOM_DOMAIN` is set to `lewiseydman.dev`.

## Remaining actions

1. Confirm in **Settings → Secrets and variables → Actions → Variables** that a variable named `CUSTOM_DOMAIN` with value `lewiseydman.dev` exists. Without it, the next deploy will wipe the custom domain in Pages settings.
2. Wait for GitHub to finish SSL provisioning and then enable **Enforce HTTPS** once the certificate check passes.
